<resources>

    <style name="Theme.MobiloSignal" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- androidx.preference needs this explicitly; without it the settings list renders
             with missing text and icon styling. -->
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>

</resources>
